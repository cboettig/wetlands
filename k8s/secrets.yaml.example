apiVersion: v1
kind: Secret
metadata:
  name: wetlands-secrets
type: Opaque
stringData:
  # NRP API key for LLM access (if needed by the static site)
  nrp-api-key: "your-nrp-api-key-here"
  
  # MCP Server URL
  mcp-server-url: "https://mcp-server.nrp-nautilus.io"
