apiVersion: v1
kind: Service
metadata:
  name: wetlands-maplibre
  labels:
    app: wetlands-maplibre
spec:
  type: ClusterIP
  selector:
    app: wetlands-maplibre
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
