apiVersion: v1
kind: Service
metadata:
  name: llm-proxy
  labels:
    app: llm-proxy
spec:
  type: ClusterIP
  selector:
    app: llm-proxy
  ports:
    - protocol: TCP
      port: 8002
      targetPort: 8002
      name: http
